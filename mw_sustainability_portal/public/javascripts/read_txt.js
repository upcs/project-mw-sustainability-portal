function read_txt(route) {
    // // Use the fetch API to get the content of 'myText.txt'
    // fetch(route)
    // .then(response => response.text()) // Extract the response body as text
    // .then(textString => {
    //     // Find the paragraph element by its ID and set its text content
    //     document.getElementById("content").textContent = textString;
    // })
    // .catch(error => {
    //     console.error('Error fetching the text file:', error);
    //     document.getElementById("content").textContent = 'Failed to load content.';
    // });
    console.log("entered read_txt");
}